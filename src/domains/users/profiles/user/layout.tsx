// @ts-nocheck
import React from "react";

export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* You can add a sidebar, header, or other layout components here */}
      {children}
    </div>
  );
}


