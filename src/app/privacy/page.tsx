// Removed duplicate top-level page to avoid conflict with /(public)/privacy
// Keeping file empty to assist cleanup; if still detected, delete this folder.
export default function PrivacyPage() { return null }
