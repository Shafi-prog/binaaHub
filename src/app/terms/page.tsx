// Removed duplicate top-level page to avoid conflict with /(public)/terms
// Keeping file empty to assist cleanup; if still detected, delete this folder.
export default function TermsPage() { return null }
